namespace WebApplication1.Domain.Entities
{
    public class TransactionDelivery : BaseEntity
    {
       // public Guid TransactionId { get; private set; }
       //// public Transaction Transaction { get; private set; }
       // public DateTime DeliveryDate { get; private set; }
       // //public ICollection<TransactionItemsDelivered> TransactionItemsDelivered { get; private set; }

       // private TransactionDelivery()
       // {
            
       // }

       // private TransactionDelivery(Guid id, Guid transactionId, DateTime deliveryDate)
       // {
       //     Id = id;
       //     TransactionId = transactionId;
       //     DeliveryDate = deliveryDate;

       // }

       // public static TransactionDelivery Create(Guid id, Guid transactionId, DateTime deliveryDate)
       // => new TransactionDelivery(id, transactionId, deliveryDate);

       // public void SoftDelete()
       // {
       //     GeneralStatus = Enums.GeneralStatus.SoftDeleted;
       // }
    }
}
